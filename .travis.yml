language: objective-c
branches:
    only:
    - master
osx_image: xcode7.2
xcode_sdk: $SDK
xcode_workspace: $WORKSPACE
xcode_scheme: $SCHEME

env:
    global:
    - WORKSPACE="Keymochi.xcworkspace"
    - SCHEME="Keymochi"
    - SDK="iphoneos9.2"
    - SDK_VERSION="9.2"
